const
        inp = 'BCDIV.inp';
        out = 'BCDIV.out';
        maxN = 25;
var
procedure init;
        var
        begin
        for i := 1 to n do
        end;
begin
        init;
        assign(input, inp);
        reset(input);
        readln(t);
        for i := 1 to t do
                begin
                readln(n, k);
                writeln(F[n,k]);
                end;
        close(input);
end.
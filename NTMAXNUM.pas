uses crt;
const
        inp='NTMAXNUM.INP';
        out='NTMAXNUM.OUT';
var
procedure nhap;
        var
                f:text;
        begin
        assign(f,inp);
        reset(f);
        readln(f,n,k);
        read(f,s);
        end;
procedure xuli;
        var
        begin
        min:=9;
        for i:= 1 to n do

        end;
begin

end;